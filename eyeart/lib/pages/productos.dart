import 'package:flutter/material.dart';

class Productos extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: new AppBar(title: Text('Productos'),),
      body: new Column(
        children: <Widget>[
          new Text('Visualizacion de productos')
        ],
      ),
    );
  }
  
}

class Lista {
  
}